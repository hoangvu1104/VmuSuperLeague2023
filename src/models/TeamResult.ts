export interface TeamResult {
    teamName: string;
    point: number;    
    won: number;    
    lost: number;    
    draw: number;    
    goalsFor: number;    
    goalsAgainst: number;
    yellowCards: number;
    redCards: number;
  }